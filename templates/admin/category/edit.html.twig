{% extends 'admin/base.html.twig' %}

{% block body %}
    <h1>{{ 'admin.category.edit.title'|trans({'%category%': category.name}) }}</h1>

    {% include 'admin/category/_form.html.twig' with {'form': form} only %}

    <a href="{{ path('admin.category.list') }}" class="btn btn-default">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
        {{ 'admin.category.edit.back_to_list'|trans }}
    </a>
{% endblock %}
