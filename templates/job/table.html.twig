<table class="table text-center">
    <thead>
    <tr>
        <th class="active text-center">{{ 'job.table.city'|trans }}</th>
        <th class="active text-center">{{ 'job.table.position'|trans }}</th>
        <th class="active text-center">{{ 'job.table.company'|trans }}</th>
    </tr>
    </thead>

    <tbody>
    {% for job in jobs %}
        <tr>
            <td>{{ job.location }}</td>
            <td>
                <a href="{{ path('job.show', {id: job.id}) }}">
                    {{ job.position }}
                </a>
            </td>
            <td>{{ job.company }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
